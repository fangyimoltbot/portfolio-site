# Portfolio Website

Minimal static portfolio for Song Fang Yi.

## Run locally

Open `index.html` in your browser.

## Deploy on GitHub Pages

Push this folder as a repository and enable Pages from `main` branch, `/ (root)`.
